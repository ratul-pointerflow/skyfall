/*eslint-disable-next-line eslint-comments/disable-enable-pair */
/* eslint-disable prettier/prettier */
export default {
  // ... your existing configuration
  plugins: [
    // eslint-disable-next-line no-undef
    postcss({
      // Enable CSS modules if you need them
      modules: true,
    }),
    // other plugins
  ],
};
